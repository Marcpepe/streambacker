{{ log_path }}/nginx/*.log {
  daily
  missingok
  rotate 15
  compress
  delaycompress
  notifempty
  create 640 {{ app_user }} {{ app_group }}
  sharedscripts

  postrotate
    [ -f {{ var_path }}/run/nginx.pid ] && kill -USR1 `cat {{ var_path }}/run/nginx.pid`
  endscript
}
