# web:
  # build: .
  # ports:
    # - '5000:5000'
mongo:
  image: mongo:latest
vendors:
  image: node:0.10
  working_dir: /mnt
  volumes:
    - .:/mnt
  command: npm install
  links:
    - mongo
  # env_file: .env

proxy:
  image: nginx:1.7
  ports:
    - '5013:5013'
  volumes:
    - devops/proxy:/etc/nginx/conf.d
    - www:/var/www
